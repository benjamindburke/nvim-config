-- create chaos in a file by treating letters and symbols as cellular automata
return {
    "eandrju/cellular-automaton.nvim",
    lazy = true,
    keys = require("nvim-config.keys.cellular-automaton"),
}
